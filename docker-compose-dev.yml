version: "3.8"
services:
  webserver:
    build:
      context: .
      dockerfile: DockerFileDev
    container_name: webserver
    restart: unless-stopped
    ports:
      - "80:80"
    networks:
      - user-city-view-ui-network
networks:
  user-city-view-ui-network:
    driver: bridge